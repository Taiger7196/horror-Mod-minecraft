# Broken Script 3.0 access transformers (Mojang official names, MC 1.21.5)

# Lets PhantomRoster inject fabricated tab-list entries (phantom players with 999ms ping).
public-f net.minecraft.network.protocol.game.ClientboundPlayerInfoUpdatePacket entries

# Lets the input glitcher rewrite the already-computed movement vector (W/S inversion).
public net.minecraft.client.player.ClientInput moveVector
